# openMSX 194a769 compatibility update — 2026-09-22

> **現在の位置付け：互換モードでの動作確認済み手順です。新モード・実機への対応を保証するものではありません。新モードの表示不良は [検証記録](NEW-MODE-STATUS.md) を参照してください。追加対応は一旦保留します。 / Verified compatibility-mode setup only; current-model and hardware support are not claimed. Further adaptation is on hold.**

V9968対応openMSXの2026年9月20〜21日更新を、従来の `d884c4b` と比較しました。**実行ファイルの差し替えだけでは、公開済み5デモすべてに表示崩れが発生します。** 今回は対応ROMと、最新版が用意する `V9968_OLD` レジスター互換設定を組み合わせます。旧エミュレーター・旧ROMはそのまま保管してください。

## 起動方法

1. [配布元](https://buppu3.github.io/)の **194a769** 版を使用します。
2. [内蔵VDP用XML](compatibility/machines/Panasonic_FS-A1ST_V9968_Demos_194a769.xml) をopenMSXの `share/machines/` に保存します。既存のマシンXMLを上書きする必要はありません。
3. マシン **Panasonic_FS-A1ST_V9968_Demos_194a769** を選び、下表のROMを **ASCII8** 指定で起動します。
4. 外付け構成では [拡張XML](compatibility/extensions/HRA_V9968_Demos_194a769.xml) を `share/extensions/` に保存し、FS-A1ST＋同拡張と、各デモの外付け用ROMを使います。映像出力は `V9968` を選びます。

BIOSは各自の既存環境のものを使用してください。同梱していません。XMLは配布元の設定を基に、VDPの指定を `V9968_OLD` に変更したものです。通常の `V9968` と、今回の互換設定を取り違えないでください。

**[対応ROM・ソース・設定の一括ZIP](outputs/V9968_SampleDemos-194a769.zip)**。動画はZIP外のGitHub上で参照できます。

| デモ | 194a769で使う内蔵VDP用ROM | 今回の対応 | 実測更新/秒 |
|---|---|---|---:|
| PRISM FLIGHT | [従来ROMを継続](outputs/PRISM_FLIGHT-V9968-legacy-openmsx-internal.rom) | XMLを互換設定に変更 | 約59.92 |
| SUPER CAT | [従来ROMを継続](demos/super-cat-coastal-flight/outputs/SUPER_CAT-COASTAL_FLIGHT-V9968-legacy-openmsx-internal.rom) | XMLを互換設定に変更 | 約59.92 |
| LUMEN / FORGE | [対応ROM](demos/lumen-forge/outputs/LUMEN_FORGE-V9968-openmsx-194a769-internal.rom) | リニアVRAM、属性転送、原画配置・表示ページ対応 | 約59.92 |
| MIST / VALE | [対応ROM](demos/mist-vale/outputs/MIST_VALE-V9968-openmsx-194a769-internal.rom) | リニアVRAM、属性転送、霧原画の配置対応 | 約59.92 |
| CATSTRIDER | [対応ROM](demos/catstrider/outputs/CATSTRIDER-V9968-openmsx-194a769-internal.rom) | リニアVRAM、属性転送、猫などの原画配置対応 | 約29.96 |

新ROMの容量は従来と同じ512KiBです。PRISM FLIGHTは128KiBのままです。新旧ROMを自動判別して切り替える仕組みではありません。

## 影響の内訳

- **新レジスター定義**：`V9968` が新定義を指し、旧定義は `V9968_OLD` になりました。加えて194a769のソースでは、新定義で `hasEVR()` がfalseになる一方、VDPコマンドの読書き窓のマスクがその値を使用しています。新定義側では128KiB超の参照に不整合が見られ、SCREEN5の2本も背景が失われました。単にROMのR20/R21初期値を変更するだけでは解消しないため、今回は旧定義を明示して回避しています。これはこのエミュレーター版での観測とソース解析であり、実機VDPの不具合を示すものではありません。
- **SP3時のVRAM Planar無効化**：SCREEN8を使う3本に直接影響します。従来の偶数／奇数画素を分離した原画を連続配置へ戻し、属性の二重書込みを通常の1回へ変更しました。原画と描画領域が重ならないようアトラスも移しました。この変更は `V9968_OLD` を選んでも必要です。
- **LFMCのR44処理**：今回の5デモはLFMCを使っていないため、この修正へのコード対応はありません。
- **初期パレット・256ドット右端の修正**：各デモはパレットを自前で設定します。右端表示の改善によって、旧録画と端の画素が一致しないことがあります。

### 新しいSCREEN8用配置

表示ページは00000h／10000h、属性表はE000h／E400hです。どちらもSCREEN8表示領域212行の外側に属性表を置きます。原画は20000hからの連続した64KiBです。

- LUMEN：変形元の4bpp原画を30000hへ移動。LRMM参照Yを1280から1536へ移し、変形先38000h／3C000hは維持します。
- MIST：木の原画は30000h、霧原画は3C000h。木と岸辺の同期、水面ラスタの演出は維持します。
- CATSTRIDER：猫・魚・猫缶などの原画は30000h。航路・敵配置・遠近係数・音のデータは維持します。

`src/demo-194a769.asm` と `tools/build_updated.py` が新しい版です。従来の `src/demo.asm` と `tools/build.py` は旧エミュレーター用として残しています。新ビルダーは既存の原画・モーション素材を読み、VRAM配置と必要な参照座標だけを変換します。

## 検証と動画

全5本を内蔵構成で各65秒、修正した3本を外付け構成でも各65秒実行しました。ループ、連続するモーション番号、描画完了、R800 DRAM、表示ページの交互切り替えを確認しました。修正3本では原画のVRAM転送、両ページの固定背景、表示中のスプライト属性とROMレコードの一致も検証しています。森と宇宙猫は連続フレームを確認し、修正前の画面崩れ・交互の欠落は見られませんでした。

- [検証記録](compatibility/verification-194a769.json)
- [LUMEN最新版の実行動画](demos/lumen-forge/outputs/LUMEN_FORGE-194a769.mp4)
- [MIST最新版の実行動画](demos/mist-vale/outputs/MIST_VALE-194a769.mp4)
- [CATSTRIDER最新版の実行動画](demos/catstrider/outputs/CATSTRIDER-194a769.mp4)

各動画は12秒、エミュレーター内蔵録画から作成した約59.92fpsのMP4です。フレーム補間や再生速度の変更はありません。CATSTRIDERのシーン更新は映像2フレームにつき1回です。旧GIFや旧ZIPはd884c4b当時の資料として残しています。実機・FPGAでの動作や性能は未検証、試作・無保証です。

## English

Updating only the emulator breaks the previous demos. **For openMSX 194a769, use the supplied V9968_OLD compatibility XML and the ROMs in the table.** PRISM FLIGHT and SUPER CAT retain their original ROMs. LUMEN, MIST and CATSTRIDER have separate new ROMs with linear SCREEN8/SP3 VRAM addressing and single-write sprite attributes. Do not use these new ROMs with d884c4b.

The default new-register V9968 model also exhibits an upper-VRAM command-access inconsistency: `hasEVR()` excludes the new model while command-window masks still depend on it. The compatibility XML avoids that path; it does not revert the new linear SP3 addressing, which is why three ROMs still need changes. This is an emulator-specific finding, not a claim about FPGA hardware.

All five internal configurations and the three revised external configurations passed 65-second runs, including loop and page checks. New 12-second videos retain the native capture cadence. Original art, movement and audio are retained. Hardware remains untested; all samples are experimental and supplied without warranty.

## 一次資料 / Sources

- [配布・更新履歴](https://buppu3.github.io/)
- [新レジスター対応](https://github.com/buppu3/openMSX/commit/251ad0c)
- [SP3とVRAM配置の変更](https://github.com/buppu3/openMSX/commit/194a769d98efd02c8a66a35e50e8e7790eb85efe)
- [LFMC修正](https://github.com/buppu3/openMSX/commit/74e0067ed13aa7a91ff7041eea50756389faf44f)
- [VDPの能力判定](https://github.com/buppu3/openMSX/blob/194a769/src/video/VDP.hh)、[コマンドのアドレス窓](https://github.com/buppu3/openMSX/blob/194a769/src/video/VDPCmdEngine.cc)
